import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  root: {
    background: "#0d0127",
  },
  divider: {
    color: "unset",
    background: "unset",
    margin: "7rem 0",
  },
}));

export default useStyles;
