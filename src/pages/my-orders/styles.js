import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "80%",
    margin: "0 auto",
    background: "#09001A",
    overflow: "hidden",
    color: "#fff",
  },
}));

export default useStyles;
