import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  imgProfile: {
    width: 50,
    height: 50,
    borderRadius: "50%",
    marginRight: "0.5rem",
  },
}));

export default useStyles;
